extends Node2D

# class member variables go here, for example:
# var a = 2
# var b = "textvar"

var frame = preload("data/images/engine/menu/frame.png")

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass
	
func draw():
	# draw_rect(Rect2(0, 0, 10, 10), Color(255, 0, 0))
	# draw_texture(frame, Vector2(0, 0))
	pass
